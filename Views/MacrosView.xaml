<UserControl x:Class="DriftLock.Views.MacrosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="850" d:DesignWidth="1050"
             Foreground="{DynamicResource TextPrimary}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Border x:Name="TrackBackground" Grid.Row="1" Height="6" Background="{DynamicResource SubCardBg}" CornerRadius="0" VerticalAlignment="Center" Margin="5,0"/>
                            <Border x:Name="PART_SelectionRange" Grid.Row="1" Height="6" Background="{DynamicResource RedNeon}" CornerRadius="0" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <Border.Effect>
                                    <DropShadowEffect Color="#FF1744" BlurRadius="6" ShadowDepth="0"/>
                                </Border.Effect>
                            </Border>
                            <Track x:Name="PART_Track" Grid.Row="1">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Opacity="0"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Opacity="0"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb" Width="14" Height="14" Background="{DynamicResource RedNeon}" BorderBrush="White" BorderThickness="2">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Fill="{TemplateBinding Background}" Stroke="{TemplateBinding BorderBrush}" StrokeThickness="{TemplateBinding BorderThickness}">
                                                    <Ellipse.Effect>
                                                        <DropShadowEffect Color="#FF1744" BlurRadius="8" ShadowDepth="0"/>
                                                    </Ellipse.Effect>
                                                </Ellipse>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Margin="12">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <!-- ##== WORKSTATION TITLE ==## -->
                <TextBlock Text="MACRO SEQUENCER &amp; SENSITIVITY WORKSTATION" FontWeight="Black" FontSize="16" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,16"/>

                <!-- ##== STICK SENSITIVITY SCALING CARD ==## -->
                <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="20" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Stick Sensitivity Scaling" Foreground="{DynamicResource RedNeon}" FontSize="13" FontWeight="Bold" Margin="0,0,0,4"/>
                        <TextBlock Text="Adjust analog stick response curve gain multiplier across games" Foreground="{DynamicResource TextSecondary}" FontSize="11" Margin="0,0,0,14"/>
                        <Grid Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="70"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Multiplier:" Foreground="{DynamicResource TextPrimary}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <Slider Grid.Column="1" Minimum="0.5" Maximum="2.0" SmallChange="0.1" TickFrequency="0.1" Value="{Binding StickSensitivity}" VerticalAlignment="Center"/>
                            <Border Grid.Column="2" Background="{DynamicResource RedNeonDark}" BorderBrush="{DynamicResource RedNeon}" BorderThickness="1" CornerRadius="0" Padding="8,3" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding StickSensitivity, StringFormat={}{0:F1}x}" Foreground="{DynamicResource TextPrimary}" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- ##== DEADZONES CONTROL CARD ==## -->
                <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="20" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Analog Stick Deadzone Limits" Foreground="{DynamicResource RedNeon}" FontSize="13" FontWeight="Bold" Margin="0,0,0,4"/>
                        <TextBlock Text="Set inner anti-drift deadzone threshold percentage" Foreground="{DynamicResource TextSecondary}" FontSize="11" Margin="0,0,0,16"/>
                        <Grid Margin="0,0,0,14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="70"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Left Stick:" Foreground="{DynamicResource TextPrimary}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <Slider Grid.Column="1" Minimum="0" Maximum="50" SmallChange="1" TickFrequency="1" Value="{Binding LeftStickDeadzone}" VerticalAlignment="Center"/>
                            <Border Grid.Column="2" Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="8,3" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding LeftStickDeadzone, StringFormat={}{0:F0}%}" Foreground="{DynamicResource RedNeon}" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="70"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Right Stick:" Foreground="{DynamicResource TextPrimary}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <Slider Grid.Column="1" Minimum="0" Maximum="50" SmallChange="1" TickFrequency="1" Value="{Binding RightStickDeadzone}" VerticalAlignment="Center"/>
                            <Border Grid.Column="2" Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="8,3" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding RightStickDeadzone, StringFormat={}{0:F0}%}" Foreground="{DynamicResource RedNeon}" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- ##== CUSTOM KEY SEQUENCE & MACRO EDITOR CARD ==## -->
                <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="20">
                    <StackPanel>
                        <!-- ##== MACRO WORKSTATION HEADER ==## -->
                        <Grid Margin="0,0,0,16">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Custom Key Sequence &amp; Macro Editor" Foreground="{DynamicResource RedNeon}" FontSize="13" FontWeight="Bold" Margin="0,0,0,2"/>
                                <TextBlock Text="Record and trigger multi-button combos with precise frame delays" Foreground="{DynamicResource TextSecondary}" FontSize="11"/>
                                <TextBlock Text="{Binding MacroErrorNotifier}" Foreground="#FF1744" FontSize="11" FontWeight="Bold" Margin="0,4,0,0"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <ComboBox ItemsSource="{Binding ActiveMacros}" SelectedItem="{Binding ActiveMacro}" DisplayMemberPath="Name" Width="170" Height="30" Margin="0,0,8,0" VerticalContentAlignment="Center"/>
                                <Button Command="{Binding CreateNewMacroCommand}" Content="+ NEW MACRO" Style="{DynamicResource DefaultButtonStyle}" Padding="10,5" Margin="0,0,8,0" FontSize="11"/>
                                <Button Command="{Binding ToggleRecordMacroCommand}" Content="{Binding RecordingStatusText}" Style="{DynamicResource RedNeonBtn}" Padding="14,5" Margin="0,0,8,0" FontSize="11"/>
                                <Button Command="{Binding PlayMacroCommand}" Content="▶ TEST" Style="{DynamicResource DefaultButtonStyle}" Padding="10,5" Margin="0,0,8,0" FontSize="11"/>
                                <Button Command="{Binding DeleteMacroCommand}" Content="🗑" Style="{DynamicResource DefaultButtonStyle}" Padding="8,5" ToolTip="Delete Selected Macro" FontSize="11"/>
                            </StackPanel>
                        </Grid>

                        <!-- ##== MACRO DETAILS EDITOR ==## -->
                        <Border Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="14" Margin="0,0,0,16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="MACRO PROFILE NAME" Foreground="{DynamicResource TextMuted}" FontSize="10" FontWeight="Bold" Margin="0,0,0,6"/>
                                    <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0">
                                        <TextBox Text="{Binding ActiveMacro.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" Foreground="{DynamicResource TextPrimary}" BorderThickness="0" Padding="8,5" FontSize="12"/>
                                    </Border>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="TRIGGER BUTTON COMBO" Foreground="{DynamicResource TextMuted}" FontSize="10" FontWeight="Bold" Margin="0,0,0,6"/>
                                    <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0">
                                        <TextBox Text="{Binding ActiveMacro.TriggerButtonName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" Foreground="{DynamicResource TextPrimary}" BorderThickness="0" Padding="8,5" FontSize="12"/>
                                    </Border>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- ##== RECORDING STATUS BANNER ==## -->
                        <Border Visibility="{Binding IsRecordingMacro, Converter={StaticResource BoolToVis}}" Background="#33FF1744" BorderBrush="{DynamicResource RedNeon}" BorderThickness="1" CornerRadius="0" Padding="12,8" Margin="0,0,0,16">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Ellipse Width="10" Height="10" Fill="{DynamicResource RedNeon}" Margin="0,0,8,0" VerticalAlignment="Center">
                                    <Ellipse.Effect>
                                        <DropShadowEffect Color="#FF1744" BlurRadius="10" ShadowDepth="0"/>
                                    </Ellipse.Effect>
                                </Ellipse>
                                <TextBlock Text="🔴 MACRO RECORDING ACTIVE: Press controller buttons to capture key sequence and delays automatically..." Foreground="{DynamicResource RedNeon}" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <!-- ##== MACRO SEQUENCE TIMELINE BOARD ==## -->
                        <Border Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="16">
                            <StackPanel>
                                <Grid Margin="0,0,0,12">
                                    <TextBlock Text="SEQUENCE TIMELINE STEPS" Foreground="{DynamicResource TextPrimary}" FontSize="11" FontWeight="Bold"/>
                                    <Button Command="{Binding AddStepToActiveMacroCommand}" Content="+ ADD STEP" Style="{DynamicResource DefaultButtonStyle}" Padding="10,4" HorizontalAlignment="Right" FontSize="10"/>
                                </Grid>

                                <!-- ##== Steps ItemsControl ==## -->
                                <ItemsControl ItemsSource="{Binding ActiveMacro.Steps}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Margin="0,0,8,12" VerticalAlignment="Center">
                                                <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource RedNeon}" BorderThickness="1" CornerRadius="0" Padding="10,8">
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <TextBlock Text="PRESS [" Foreground="{DynamicResource TextMuted}" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                                                        <TextBox Text="{Binding ButtonName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" Foreground="{DynamicResource RedNeon}" BorderThickness="0" FontWeight="Bold" FontSize="11" VerticalAlignment="Center"/>
                                                        <TextBlock Text="]" Foreground="{DynamicResource TextMuted}" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                        <Button Command="{Binding DataContext.RemoveStepFromActiveMacroCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Background="Transparent" Foreground="{DynamicResource TextMuted}" BorderThickness="0" Cursor="Hand" ToolTip="Remove Step">
                                                            <TextBlock Text="✕" FontSize="10" FontWeight="Bold"/>
                                                        </Button>
                                                    </StackPanel>
                                                </Border>
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="6,0,0,0">
                                                    <TextBlock Text="➔" Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center" FontSize="11" Margin="0,0,4,0"/>
                                                    <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="6,2">
                                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                            <TextBox Text="{Binding DelayMs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" Foreground="{DynamicResource TextPrimary}" BorderThickness="0" FontSize="10" FontWeight="Bold" Width="28" TextAlignment="Center"/>
                                                            <TextBlock Text="ms" Foreground="{DynamicResource TextMuted}" FontSize="9" VerticalAlignment="Center"/>
                                                        </StackPanel>
                                                    </Border>
                                                    <TextBlock Text="➔" Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center" FontSize="11" Margin="4,0,0,0"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <Separator Background="{DynamicResource BorderBrush}" Margin="0,8,0,12"/>
                                <TextBlock Text="ⓘ Sequences execute through the high-precision virtual bus with sub-millisecond accuracy." Foreground="{DynamicResource TextMuted}" FontSize="10"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>