<UserControl x:Class="DriftLift.Views.ProfilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1050"
             Foreground="{DynamicResource TextPrimary}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Header Bar -->
        <Border Grid.Row="0" Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="18,12" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0">
                    <TextBlock Text="GAME PROFILES &amp; AUTO-SWITCHING WORKSTATION" FontWeight="Black" FontSize="14" Foreground="{DynamicResource TextPrimary}" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="Drift Lift dynamically activates game-specific deadzones and curves the exact millisecond your game window gains focus" Foreground="{DynamicResource TextSecondary}" FontSize="10" TextWrapping="Wrap" Margin="0,3,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Command="{Binding ScanInstalledGamesCommand}" Content="🔍 SCAN INSTALLED GAMES" Style="{DynamicResource RedNeonBtn}" Padding="14,7" FontSize="10" FontWeight="Bold" Margin="0,0,6,0"/>
                    <Button Command="{Binding AddCustomGameCommand}" Content="+ ADD CUSTOM" Style="{DynamicResource DefaultButtonStyle}" Padding="12,7" FontSize="10" Margin="0,0,6,0"/>
                    <Button Command="{Binding ResetGameProfilesCommand}" Content="↺ CLEAR" Style="{DynamicResource DefaultButtonStyle}" Padding="10,7" FontSize="10"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Workstation Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Linked Games List / Empty State -->
            <Border Grid.Column="0" Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="14" Margin="0,0,14,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="LINKED GAMES" Foreground="{DynamicResource RedNeon}" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding GameProfiles.Count, StringFormat='({0})'}" Foreground="{DynamicResource TextMuted}" FontSize="10" FontWeight="Bold" VerticalAlignment="Center"/>
                    </Grid>
                    <Grid Grid.Row="1">
                        <!-- Empty State Card -->
                        <Border Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="18" VerticalAlignment="Center" HorizontalAlignment="Stretch">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding GameProfiles.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel HorizontalAlignment="Center">
                                <Path Data="{StaticResource IconControllerVector}" Fill="{DynamicResource RedNeon}" Width="36" Height="36" Stretch="Uniform" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                <TextBlock Text="NO GAMES LINKED" FontWeight="Black" FontSize="12" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Center"/>
                                <TextBlock Text="Scan your PC to automatically detect installed games from Steam, EA, Epic, and other libraries." TextWrapping="Wrap" TextAlignment="Center" Foreground="{DynamicResource TextSecondary}" FontSize="10" Margin="0,6,0,14" MaxWidth="280"/>
                                <Button Command="{Binding ScanInstalledGamesCommand}" Content="🔍 SCAN INSTALLED GAMES" Style="{DynamicResource RedNeonBtn}" Padding="14,7" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <Button Command="{Binding AddCustomGameCommand}" Content="+ ADD MANUALLY" Style="{DynamicResource DefaultButtonStyle}" Padding="12,6" FontSize="10" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <!-- Populated Games List -->
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ScrollViewer.Style>
                                <Style TargetType="ScrollViewer">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding GameProfiles.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ScrollViewer.Style>
                            <ItemsControl ItemsSource="{Binding GameProfiles}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="10,8" Margin="0,0,0,6" Cursor="Hand">
                                            <Border.InputBindings>
                                                <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.SelectGameProfileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                                            </Border.InputBindings>
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                            <Setter Property="BorderBrush" Value="{DynamicResource RedNeon}"/>
                                                            <Setter Property="Background" Value="#22FF1744"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="32"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <!-- Game Icon / Fallback Vector Controller -->
                                                <Grid Grid.Column="0" Width="28" Height="28" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <Path Data="{StaticResource IconControllerVector}" Fill="{DynamicResource RedNeon}" Width="20" Height="20" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                        <Path.Style>
                                                           <Style TargetType="Path">
                                                               <Style.Triggers>
                                                                   <DataTrigger Binding="{Binding GameIcon}" Value="{x:Null}">
                                                                       <Setter Property="Visibility" Value="Visible"/>
                                                                   </DataTrigger>
                                                               </Style.Triggers>
                                                               <Setter Property="Visibility" Value="Collapsed"/>
                                                           </Style>
                                                        </Path.Style>
                                                    </Path>
                                                    <Image Source="{Binding GameIcon}" Width="26" Height="26" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality">
                                                        <Image.Style>
                                                           <Style TargetType="Image">
                                                               <Style.Triggers>
                                                                   <DataTrigger Binding="{Binding GameIcon}" Value="{x:Null}">
                                                                       <Setter Property="Visibility" Value="Collapsed"/>
                                                                   </DataTrigger>
                                                               </Style.Triggers>
                                                               <Setter Property="Visibility" Value="Visible"/>
                                                           </Style>
                                                        </Image.Style>
                                                    </Image>
                                                </Grid>

                                                <StackPanel Grid.Column="1" Margin="8,0,8,0" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding GameName}" Foreground="{DynamicResource TextPrimary}" FontSize="11" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                                    <TextBlock Text="{Binding ExecutableName}" Foreground="{DynamicResource TextMuted}" FontSize="9" TextTrimming="CharacterEllipsis"/>
                                                </StackPanel>
                                                <Border Grid.Column="2" VerticalAlignment="Center" CornerRadius="0" Padding="5,2">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="{DynamicResource SubCardBg}"/>
                                                            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                                            <Setter Property="BorderThickness" Value="1"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                                    <Setter Property="Background" Value="#FF1744"/>
                                                                    <Setter Property="BorderBrush" Value="#FF1744"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock FontSize="8" FontWeight="Bold">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Text" Value="READY"/>
                                                                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                                        <Setter Property="Text" Value="● ACTIVE"/>
                                                                        <Setter Property="Foreground" Value="White"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </Border>

            <!-- Right: Detail Workstation -->
            <Border Grid.Column="1" Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="20">
                <Grid>
                    <!-- Empty selection placeholder -->
                    <Border Background="Transparent" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedGameProfile}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel HorizontalAlignment="Center">
                            <Path Data="{StaticResource IconControllerVector}" Fill="{DynamicResource TextMuted}" Width="48" Height="48" Stretch="Uniform" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                            <TextBlock Text="SELECT OR ADD A GAME" Foreground="{DynamicResource TextSecondary}" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="Choose a game from the left panel or click 'SCAN INSTALLED GAMES' to begin." Foreground="{DynamicResource TextMuted}" FontSize="11" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Selected Game Editor -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ScrollViewer.Style>
                            <Style TargetType="ScrollViewer">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedGameProfile}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ScrollViewer.Style>
                        <StackPanel>
                            <Grid Margin="0,0,0,16">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <!-- Clickable Logo Picker -->
                                    <Button Command="{Binding ChangeSelectedGameLogoCommand}" Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="0" Margin="0,0,12,0" VerticalAlignment="Center" ToolTip="Click to upload custom logo">
                                        <Border Width="44" Height="44" Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0">
                                            <Grid>
                                                <Path Data="{StaticResource IconControllerVector}" Fill="{DynamicResource RedNeon}" Width="26" Height="26" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <Path.Style>
                                                        <Style TargetType="Path">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding SelectedGameProfile.GameIcon}" Value="{x:Null}">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </Style>
                                                    </Path.Style>
                                                </Path>
                                                <Image Source="{Binding SelectedGameProfile.GameIcon}" Width="38" Height="38" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality">
                                                    <Image.Style>
                                                        <Style TargetType="Image">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding SelectedGameProfile.GameIcon}" Value="{x:Null}">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </Style>
                                                    </Image.Style>
                                                </Image>
                                            </Grid>
                                        </Border>
                                    </Button>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding SelectedGameProfile.GameName, FallbackValue='No Game Selected'}" Foreground="{DynamicResource TextPrimary}" FontSize="15" FontWeight="Black"/>
                                        <TextBlock Text="{Binding SelectedGameProfile.Category, StringFormat='Category: {0}', FallbackValue=''}" Foreground="{DynamicResource RedNeon}" FontSize="10" FontWeight="Bold" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <Button Command="{Binding ActivateSelectedGameProfileCommand}" Content="⚡ ACTIVATE PROFILE" Style="{DynamicResource RedNeonBtn}" Padding="14,6" FontSize="11" Margin="0,0,8,0"/>
                                    <Button Command="{Binding DeleteSelectedGameProfileCommand}" Content="🗑 DELETE" Style="{DynamicResource DefaultButtonStyle}" Padding="10,6" FontSize="11"/>
                                </StackPanel>
                            </Grid>

                            <Separator Background="{DynamicResource BorderBrush}" Margin="0,0,0,16"/>

                            <TextBlock Text="EXECUTABLE LINKAGE" Foreground="{DynamicResource RedNeon}" FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Margin="0,0,8,0">
                                    <TextBox Text="{Binding SelectedGameProfile.ExecutableName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" Foreground="{DynamicResource TextPrimary}" BorderThickness="0" Padding="10,6" FontSize="11" VerticalContentAlignment="Center"/>
                                </Border>
                                <Button Grid.Column="1" Command="{Binding BrowseGameExeCommand}" Content="BROWSE .EXE" Style="{DynamicResource DefaultButtonStyle}" Padding="14,6" FontSize="11"/>
                            </Grid>

                            <CheckBox Content="Enable Auto-Switching for this Game (Triggers on Window Focus)" IsChecked="{Binding SelectedGameProfile.IsAutoSwitchEnabled, Mode=TwoWay}" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold" FontSize="11" Margin="0,0,0,20"/>

                            <TextBlock Text="CALIBRATION &amp; DEADZONE TUNING" Foreground="{DynamicResource RedNeon}" FontSize="11" FontWeight="Bold" Margin="0,0,0,12"/>
                            <Border Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="16" Margin="0,0,0,16">
                                <StackPanel>
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="130"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="60"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Left Stick Deadzone:" Foreground="{DynamicResource TextPrimary}" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        <Slider Grid.Column="1" Minimum="0" Maximum="40" SmallChange="1" Value="{Binding SelectedGameProfile.LeftStickDeadzone, Mode=TwoWay}" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="2" Text="{Binding SelectedGameProfile.LeftStickDeadzone, StringFormat='{}{0:F0}%'}" Foreground="{DynamicResource RedNeon}" FontSize="11" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                    </Grid>
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="130"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="60"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Right Stick Deadzone:" Foreground="{DynamicResource TextPrimary}" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        <Slider Grid.Column="1" Minimum="0" Maximum="40" SmallChange="1" Value="{Binding SelectedGameProfile.RightStickDeadzone, Mode=TwoWay}" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="2" Text="{Binding SelectedGameProfile.RightStickDeadzone, StringFormat='{}{0:F0}%'}" Foreground="{DynamicResource RedNeon}" FontSize="11" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                    </Grid>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="130"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="60"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Stick Sensitivity:" Foreground="{DynamicResource TextPrimary}" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        <Slider Grid.Column="1" Minimum="0.5" Maximum="2.0" SmallChange="0.1" Value="{Binding SelectedGameProfile.StickSensitivity, Mode=TwoWay}" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="2" Text="{Binding SelectedGameProfile.StickSensitivity, StringFormat='{}{0:F1}x'}" Foreground="{DynamicResource RedNeon}" FontSize="11" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <TextBlock Text="AUTO-SWITCHING INTEGRATION NOTICE" Foreground="{DynamicResource TextMuted}" FontSize="10" FontWeight="Bold" Margin="0,0,0,4"/>
                            <TextBlock Text="When you switch focus to this game, Drift Lift instantly applies these custom stick tolerances and displays a notification. When you return to Desktop, it smoothly reverts back to the Default Profile." Foreground="{DynamicResource TextSecondary}" FontSize="10" TextWrapping="Wrap"/>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
