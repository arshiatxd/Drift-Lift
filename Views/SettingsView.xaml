<UserControl x:Class="DriftLift.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="950"
             Foreground="{DynamicResource TextPrimary}">
    <UserControl.Resources>
        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="{DynamicResource SubCardBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#22FF1744"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource RedNeon}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#44FF1744"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource RedNeon}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Margin="12">
        <ScrollViewer VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <TextBlock Text="APPLICATION SETTINGS &amp; SYSTEM PREFERENCES" FontWeight="Black" FontSize="16" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,16"/>
                
                <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="20" Margin="0,0,0,16">
                    <Grid>
                        <StackPanel VerticalAlignment="Center" Margin="0,0,220,0">
                            <TextBlock Text="Theme Appearance &amp; Color System" Foreground="{DynamicResource RedNeon}" FontSize="13" FontWeight="Bold" Margin="0,0,4,0"/>
                            <TextBlock Text="Switch between AMOLED Dark Red (#000000) and Light Mode aesthetics" Foreground="{DynamicResource TextSecondary}" FontSize="11"/>
                        </StackPanel>
                        <Button Command="{Binding ToggleThemeCommand}" Content="TOGGLE LIGHT / DARK THEME" Style="{DynamicResource RedNeonBtn}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
                
                <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="20" Margin="0,0,0,16">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Saved Calibration &amp; Profile Presets" Foreground="{DynamicResource RedNeon}" FontSize="13" FontWeight="Bold" Margin="0,0,2,0"/>
                                <TextBlock Text="Manage custom stored configuration files saved to your disk" Foreground="{DynamicResource TextSecondary}" FontSize="11"/>
                            </StackPanel>
                            <Button Command="{Binding SaveConfigPresetCommand}" Content="+ SAVE CURRENT PRESET" Style="{DynamicResource RedNeonBtn}" HorizontalAlignment="Right" Padding="14,6"/>
                        </Grid>
                        <ListBox ItemsSource="{Binding SavedConfigFiles}" Background="Transparent" BorderThickness="0" Height="130"/>
                    </StackPanel>
                </Border>
                
                <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="20" Margin="0,0,0,16">
                    <Grid>
                        <StackPanel VerticalAlignment="Center" Margin="0,0,180,0">
                            <TextBlock Text="Double Input Controller Fix (HidHide)" Foreground="{DynamicResource RedNeon}" FontSize="13" FontWeight="Bold" Margin="0,0,4,0"/>
                            <TextBlock Text="Prevents double input conflicts by hiding physical Bluetooth / USB controllers from games while routing through virtual Xbox bus. Driver Status:" Foreground="{DynamicResource TextSecondary}" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,6"/>
                            <Border Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="10,6" HorizontalAlignment="Left" Margin="0,0,0,10">
                                <TextBlock Text="{Binding HidHideStatusText}" Foreground="{Binding HidHideStatusColor}" FontSize="11" FontWeight="Bold"/>
                            </Border>
                            <CheckBox Content="Enable Hardware Controller Hiding" IsChecked="{Binding IsHidHideEnabled}" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold" FontSize="11"/>
                        </StackPanel>
                        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Vertical">
                            <Button Command="{Binding AutoShieldPlayStationControllersCommand}" Content="⚡ AUTO-SHIELD PS PADS" Style="{DynamicResource RedNeonBtn}" Margin="0,0,0,6" Width="180" Height="32"/>
                            <Button Command="{Binding InstallHidHideCommand}" Content="OPEN CONFIGURATOR" Style="{DynamicResource DefaultButtonStyle}" Margin="0,0,0,6" Width="180" Height="30"/>
                            <Button Command="{Binding CheckHidHideCommand}" Content="REFRESH STATUS" Style="{DynamicResource DefaultButtonStyle}" Width="180" Height="30"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="20" Margin="0,0,0,16">
                    <Grid>
                        <StackPanel VerticalAlignment="Center" Margin="0,0,60,0">
                            <TextBlock Text="Virtual Xbox Controller Emulation (ViGEmBus Output)" Foreground="{DynamicResource RedNeon}" FontSize="13" FontWeight="Bold" Margin="0,0,4,0"/>
                            <TextBlock Text="Emits calibrated and remapped inputs to a virtual Xbox 360 controller. When using a PlayStation controller, ensure HidHide is enabled above so games do not receive double inputs. Disable this toggle if you want direct native controller passthrough." Foreground="{DynamicResource TextSecondary}" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,8"/>
                            <CheckBox Content="Enable Virtual Xbox 360 Output (ViGEm)" IsChecked="{Binding IsVirtualOutputEnabled}" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold" FontSize="11"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="20" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="System Integration &amp; Background Lifecycle" Foreground="{DynamicResource RedNeon}" FontSize="13" FontWeight="Bold" Margin="0,0,4,0"/>
                        <TextBlock Text="Configure window close behaviors, auto-startup on boot, and background tray execution" Foreground="{DynamicResource TextSecondary}" FontSize="11" Margin="0,0,0,12"/>
                        <CheckBox Content="Minimize to System Tray on Close (keep anti-drift active)" IsChecked="{Binding MinimizeToTrayOnClose}" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold" FontSize="11" Margin="0,0,0,8"/>
                        <CheckBox Content="Launch Drift Lift on Windows Startup" IsChecked="{Binding StartWithWindows}" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold" FontSize="11" Margin="0,0,0,8"/>
                        <CheckBox Content="Start Minimized in System Tray on Boot" IsChecked="{Binding StartMinimized}" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold" FontSize="11"/>
                    </StackPanel>
                </Border>
                
                <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="20" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Application Details &amp; Engine Status" Foreground="{DynamicResource RedNeon}" FontSize="13" FontWeight="Bold" Margin="0,0,0,10"/>
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="SOFTWARE VERSION" Foreground="{DynamicResource TextMuted}" FontSize="11"/>
                            <TextBlock Text="Drift Lift v1.0.7 (.NET 10.0 Win-x64)" Foreground="{DynamicResource TextPrimary}" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="FRAMEWORK" Foreground="{DynamicResource TextMuted}" FontSize="11"/>
                            <TextBlock Text="Microsoft .NET 10.0 Windows Desktop Runtime" Foreground="{DynamicResource TextPrimary}" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid>
                            <TextBlock Text="VIRTUAL CONTROLLER BUS" Foreground="{DynamicResource TextMuted}" FontSize="11"/>
                            <TextBlock Text="Nefarius ViGEmBus Client" Foreground="{DynamicResource TextPrimary}" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="20">
                    <StackPanel>
                        <TextBlock Text="CREDITS &amp; ACKNOWLEDGMENTS" Foreground="{DynamicResource RedNeon}" FontSize="13" FontWeight="Bold" Margin="0,0,0,8"/>
                        <TextBlock Text="Created &amp; Developed by arshiatxd" Foreground="{DynamicResource TextPrimary}" FontSize="13" FontWeight="Bold" Margin="0,0,0,4"/>
                        <TextBlock Text="Special thanks to open-source controller calibration projects and research (DualShock Tools, ViGEm, Vortice.XInput, HidLibrary)." Foreground="{DynamicResource TextSecondary}" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,10"/>
                        <TextBlock Text="© 2026 arshiatxd. All rights reserved." Foreground="{DynamicResource TextMuted}" FontSize="10"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>