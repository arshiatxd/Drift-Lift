<UserControl x:Class="DriftLock.Views.RemapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:DriftLock.Views.Controls"
             mc:Ignorable="d" d:DesignHeight="850" d:DesignWidth="1200" Foreground="{DynamicResource TextPrimary}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style x:Key="TabBtn" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd" BorderBrush="Transparent" BorderThickness="0,0,0,2" Padding="15,10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource RedNeon}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#444444"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="MappingCombo" TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="Background" Value="{DynamicResource CardBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Height" Value="28"/>
        </Style>
        <Style x:Key="ClearBtn" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SubCardBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="0">
                            <TextBlock Text="✕" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource RedNeon}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource RedNeon}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Border Panel.ZIndex="100" Background="#CC000000" Visibility="{Binding IsWaitingForInput, Converter={StaticResource BoolToVis}}" CornerRadius="0">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="WAITING FOR USER's INPUT" FontWeight="Black" FontSize="24" Foreground="{DynamicResource RedNeon}" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="{Binding WaitingTargetText}" FontSize="16" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                <Button Content="CANCEL" Command="{Binding CancelRemapCommand}" Width="150" Style="{DynamicResource PrimaryButtonStyle}"/>
            </StackPanel>
        </Border>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> 
                <RowDefinition Height="Auto"/> 
                <RowDefinition Height="*"/>    
                <RowDefinition Height="250"/>  
            </Grid.RowDefinitions>
            
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Profile:" Foreground="{DynamicResource TextSecondary}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox Width="200" SelectedIndex="0" Style="{StaticResource MappingCombo}">
                        <ComboBoxItem Content="Default Profile"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="💡 LED LIGHTS" Style="{DynamicResource RedNeonBtn}" Padding="15,6" Margin="5,0" Command="{Binding OpenPsLedSettingsCommand}" Visibility="{Binding IsPlayStation, Converter={StaticResource BoolToVis}}" ToolTip="Customize PlayStation Controller Lightbar LED Color &amp; Brightness"/>
                    <Button Content="+ NEW PROFILE" Style="{DynamicResource DefaultButtonStyle}" Padding="15,6" Margin="5,0" Command="{Binding NewProfileCommand}"/>
                    <Button Content="💾 LOAD" Style="{DynamicResource DefaultButtonStyle}" Padding="15,6" Margin="5,0" Command="{Binding LoadProfileCommand}"/>
                    <Button Content="💾 SAVE" Style="{DynamicResource DefaultButtonStyle}" Padding="15,6" Margin="5,0" Command="{Binding SaveProfileCommand}"/>
                    <Button Content="↑ IMPORT" Style="{DynamicResource DefaultButtonStyle}" Padding="15,6" Margin="5,0" Command="{Binding ImportProfileCommand}"/>
                    <Button Content="→ EXPORT" Style="{DynamicResource DefaultButtonStyle}" Padding="15,6" Margin="5,0" Command="{Binding ExportProfileCommand}"/>
                    <Button Content="⚙ AUTO MAP" Style="{DynamicResource DefaultButtonStyle}" Padding="15,6" Margin="5,0" Command="{Binding AutoMapCommand}"/>
                </StackPanel>
            </Grid>
            
            <Grid Grid.Row="2" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1.4*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                
                <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,10,0">
                    <StackPanel>
                        
                        <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="12" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="FACE BUTTONS" Foreground="{DynamicResource RedNeon}" FontWeight="Bold" FontSize="11" Margin="0,0,0,10"/>
                                <ItemsControl ItemsSource="{Binding FaceButtonsRemap}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="90"/>
                                                    <ColumnDefinition Width="20"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="6,4" CornerRadius="0">
                                                    <TextBlock Text="{Binding SourceButtonName}" Foreground="{DynamicResource TextPrimary}" FontSize="11" FontWeight="Bold"/>
                                                </Border>
                                                <TextBlock Grid.Column="1" Text="→" Foreground="{DynamicResource TextSecondary}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                <ComboBox Grid.Column="2" ItemsSource="{Binding TargetOptions}" SelectedItem="{Binding SelectedTarget, Mode=TwoWay}" Style="{StaticResource MappingCombo}"/>
                                                <Button Grid.Column="3" Style="{StaticResource ClearBtn}" Command="{Binding ListenCommand}" ToolTip="Scan physical button" Margin="4,0,0,0">
                                                    <TextBlock Text="🎯" FontSize="10"/>
                                                </Button>
                                                <Button Grid.Column="4" Style="{StaticResource ClearBtn}" Command="{Binding ResetCommand}" ToolTip="Reset mapping" Margin="4,0,0,0"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                        
                        <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="12">
                            <StackPanel>
                                <TextBlock Text="SHOULDERS &amp; TRIGGERS" Foreground="{DynamicResource RedNeon}" FontWeight="Bold" FontSize="11" Margin="0,0,0,10"/>
                                <ItemsControl ItemsSource="{Binding ShouldersRemap}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="90"/>
                                                    <ColumnDefinition Width="20"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="6,4" CornerRadius="0">
                                                    <TextBlock Text="{Binding SourceButtonName}" Foreground="{DynamicResource TextPrimary}" FontSize="11" FontWeight="Bold"/>
                                                </Border>
                                                <TextBlock Grid.Column="1" Text="→" Foreground="{DynamicResource TextSecondary}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                <ComboBox Grid.Column="2" ItemsSource="{Binding TargetOptions}" SelectedItem="{Binding SelectedTarget, Mode=TwoWay}" Style="{StaticResource MappingCombo}"/>
                                                <Button Grid.Column="3" Style="{StaticResource ClearBtn}" Command="{Binding ListenCommand}" ToolTip="Scan physical button" Margin="4,0,0,0">
                                                    <TextBlock Text="🎯" FontSize="10"/>
                                                </Button>
                                                <Button Grid.Column="4" Style="{StaticResource ClearBtn}" Command="{Binding ResetCommand}" ToolTip="Reset mapping" Margin="4,0,0,0"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
                
                <Grid Grid.Column="1" Margin="5,0">
                    <Viewbox HorizontalAlignment="Center" VerticalAlignment="Center">
                        <controls:ControllerVisualizerControl />
                    </Viewbox>
                </Grid>
                
                <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto" Margin="10,0,0,0">
                    <StackPanel>
                        
                        <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="12" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="DIRECTIONAL D-PAD" Foreground="{DynamicResource RedNeon}" FontWeight="Bold" FontSize="11" Margin="0,0,0,10"/>
                                <ItemsControl ItemsSource="{Binding DPadRemap}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="90"/>
                                                    <ColumnDefinition Width="20"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="6,4" CornerRadius="0">
                                                    <TextBlock Text="{Binding SourceButtonName}" Foreground="{DynamicResource TextPrimary}" FontSize="11" FontWeight="Bold"/>
                                                </Border>
                                                <TextBlock Grid.Column="1" Text="→" Foreground="{DynamicResource TextSecondary}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                <ComboBox Grid.Column="2" ItemsSource="{Binding TargetOptions}" SelectedItem="{Binding SelectedTarget, Mode=TwoWay}" Style="{StaticResource MappingCombo}"/>
                                                <Button Grid.Column="3" Style="{StaticResource ClearBtn}" Command="{Binding ListenCommand}" ToolTip="Scan physical button" Margin="4,0,0,0">
                                                    <TextBlock Text="🎯" FontSize="10"/>
                                                </Button>
                                                <Button Grid.Column="4" Style="{StaticResource ClearBtn}" Command="{Binding ResetCommand}" ToolTip="Reset mapping" Margin="4,0,0,0"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                        
                        <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="12">
                            <StackPanel>
                                <TextBlock Text="STICKS &amp; SYSTEM" Foreground="{DynamicResource RedNeon}" FontWeight="Bold" FontSize="11" Margin="0,0,0,10"/>
                                <ItemsControl ItemsSource="{Binding SpecialSticksRemap}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="90"/>
                                                    <ColumnDefinition Width="20"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="6,4" CornerRadius="0">
                                                    <TextBlock Text="{Binding SourceButtonName}" Foreground="{DynamicResource TextPrimary}" FontSize="11" FontWeight="Bold"/>
                                                </Border>
                                                <TextBlock Grid.Column="1" Text="→" Foreground="{DynamicResource TextSecondary}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                <ComboBox Grid.Column="2" ItemsSource="{Binding TargetOptions}" SelectedItem="{Binding SelectedTarget, Mode=TwoWay}" Style="{StaticResource MappingCombo}"/>
                                                <Button Grid.Column="3" Style="{StaticResource ClearBtn}" Command="{Binding ListenCommand}" ToolTip="Scan physical button" Margin="4,0,0,0">
                                                    <TextBlock Text="🎯" FontSize="10"/>
                                                </Button>
                                                <Button Grid.Column="4" Style="{StaticResource ClearBtn}" Command="{Binding ResetCommand}" ToolTip="Reset mapping" Margin="4,0,0,0"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
            
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0" Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Margin="0,0,10,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <Grid Grid.Row="0" Margin="15,10,15,10">
                            <TextBlock Text="MAPPING LIST" Foreground="{DynamicResource RedNeon}" FontWeight="Bold" FontSize="11" VerticalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="+ ADD MAPPING" Style="{DynamicResource DefaultButtonStyle}" Background="{DynamicResource BgDark}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="10,4" Margin="0,0,5,0" Command="{Binding AddMappingCommand}"/>
                                <Button Content="⛭ ADD MACRO" Style="{DynamicResource DefaultButtonStyle}" Background="{DynamicResource BgDark}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="10,4" Margin="0,0,5,0" Command="{Binding AddMacroCommand}"/>
                                <Button Content="🗑 CLEAR ALL" Command="{Binding ClearRemapsCommand}" Style="{DynamicResource DefaultButtonStyle}" Background="{DynamicResource BgDark}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="10,4"/>
                            </StackPanel>
                        </Grid>
                        
                        <Border Grid.Row="1" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,1" Padding="15,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="SOURCE" Foreground="{DynamicResource TextMuted}" FontSize="10" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="OUTPUT" Foreground="{DynamicResource TextMuted}" FontSize="10" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="2" Text="TYPE" Foreground="{DynamicResource TextMuted}" FontSize="10" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="3" Text="TURBO" Foreground="{DynamicResource TextMuted}" FontSize="10" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="4" Text="MACRO" Foreground="{DynamicResource TextMuted}" FontSize="10" FontWeight="SemiBold"/>
                            </Grid>
                        </Border>
                        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding ActiveMappings}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="15,5,15,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="150"/>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="150"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                                <TextBlock Text="⭕ " Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
                                                <TextBlock Text="{Binding SourceButton}" Foreground="{DynamicResource TextPrimary}" FontSize="11" VerticalAlignment="Center"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                <TextBlock Text="→" Foreground="{DynamicResource TextSecondary}" Margin="0,0,10,0"/>
                                                <TextBlock Text="{Binding TargetButton}" Foreground="{DynamicResource TextPrimary}" FontSize="11" VerticalAlignment="Center"/>
                                            </StackPanel>
                                            <TextBlock Grid.Column="2" Text="{Binding MappingType}" Foreground="{DynamicResource TextSecondary}" FontSize="11" VerticalAlignment="Center"/>
                                            <ComboBox Grid.Column="3" SelectedItem="{Binding TurboMode}" Style="{StaticResource MappingCombo}" Margin="0,0,10,0">
                                                <ComboBoxItem Content="None"/>
                                                <ComboBoxItem Content="Rapid (20/s)"/>
                                            </ComboBox>
                                            <ComboBox Grid.Column="4" SelectedItem="{Binding MacroName}" Style="{StaticResource MappingCombo}" Margin="0,0,10,0">
                                                <ComboBoxItem Content="None"/>
                                                <ComboBoxItem Content="MULTI-KEY"/>
                                            </ComboBox>
                                            <Button Grid.Column="5" Style="{StaticResource ClearBtn}"
                                                    Command="{Binding DataContext.RemoveMappingCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
                
                <Border Grid.Column="1" Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Margin="10,0,0,0" Padding="15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="INPUT TESTING" Foreground="{DynamicResource RedNeon}" FontWeight="Bold" FontSize="11" Margin="0,0,0,15"/>
                        <StackPanel Grid.Row="1" Margin="0,0,0,15">
                            <TextBlock Text="Buttons State" Foreground="{DynamicResource TextSecondary}" FontSize="10" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" HorizontalAlignment="Center" ToolTip="L2 / LT">
                                    <Border Width="20" Height="20" Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Margin="0,0,0,4">
                                        <TextBlock Text="{Binding BtnLabelL2OrLT}" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource RedNeon}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <Ellipse Width="10" Height="10" Fill="{Binding IsL2Pressed, Converter={StaticResource BoolToColorConverter}}" HorizontalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" HorizontalAlignment="Center" ToolTip="R2 / RT">
                                    <Border Width="20" Height="20" Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Margin="0,0,0,4">
                                        <TextBlock Text="{Binding BtnLabelR2OrRT}" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource RedNeon}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <Ellipse Width="10" Height="10" Fill="{Binding IsR2Pressed, Converter={StaticResource BoolToColorConverter}}" HorizontalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" HorizontalAlignment="Center" ToolTip="L1 / LB">
                                    <Border Width="20" Height="20" Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Margin="0,0,0,4">
                                        <TextBlock Text="{Binding BtnLabelL1OrLB}" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource RedNeon}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <Ellipse Width="10" Height="10" Fill="{Binding IsL1Pressed, Converter={StaticResource BoolToColorConverter}}" HorizontalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Grid.Column="3" HorizontalAlignment="Center" ToolTip="R1 / RB">
                                    <Border Width="20" Height="20" Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Margin="0,0,0,4">
                                        <TextBlock Text="{Binding BtnLabelR1OrRB}" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource RedNeon}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <Ellipse Width="10" Height="10" Fill="{Binding IsR1Pressed, Converter={StaticResource BoolToColorConverter}}" HorizontalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Grid.Column="4" HorizontalAlignment="Center" ToolTip="Triangle / Y">
                                    <Border Width="20" Height="20" Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Margin="0,0,0,4">
                                        <TextBlock Text="{Binding BtnLabelTriangleOrY}" FontSize="11" FontWeight="Bold" Foreground="#00E676" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <Ellipse Width="10" Height="10" Fill="{Binding IsYPressed, Converter={StaticResource BoolToColorConverter}}" HorizontalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Grid.Column="5" HorizontalAlignment="Center" ToolTip="Circle / B">
                                    <Border Width="20" Height="20" Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Margin="0,0,0,4">
                                        <TextBlock Text="{Binding BtnLabelCircleOrB}" FontSize="11" FontWeight="Bold" Foreground="#FF1744" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <Ellipse Width="10" Height="10" Fill="{Binding IsBPressed, Converter={StaticResource BoolToColorConverter}}" HorizontalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Grid.Column="6" HorizontalAlignment="Center" ToolTip="Cross / A">
                                    <Border Width="20" Height="20" Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Margin="0,0,0,4">
                                        <TextBlock Text="{Binding BtnLabelCrossOrA}" FontSize="11" FontWeight="Bold" Foreground="#29B6F6" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <Ellipse Width="10" Height="10" Fill="{Binding IsAPressed, Converter={StaticResource BoolToColorConverter}}" HorizontalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Grid.Column="7" HorizontalAlignment="Center" ToolTip="Square / X">
                                    <Border Width="20" Height="20" Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Margin="0,0,0,4">
                                        <TextBlock Text="{Binding BtnLabelSquareOrX}" FontSize="11" FontWeight="Bold" Foreground="#E040FB" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <Ellipse Width="10" Height="10" Fill="{Binding IsXPressed, Converter={StaticResource BoolToColorConverter}}" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Left Stick" Foreground="{DynamicResource TextSecondary}" FontSize="10"/>
                                <Grid Margin="0,4,0,4">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding LeftStickTextX}" Foreground="{DynamicResource TextSecondary}" FontSize="10" VerticalAlignment="Center"/>
                                    <ProgressBar Grid.Column="1" Height="4" Value="{Binding LeftStickValX}" Maximum="100" Background="{DynamicResource SubCardBg}" Foreground="{DynamicResource RedNeon}" BorderThickness="0"/>
                                </Grid>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding LeftStickTextY}" Foreground="{DynamicResource TextSecondary}" FontSize="10" VerticalAlignment="Center"/>
                                    <ProgressBar Grid.Column="1" Height="4" Value="{Binding LeftStickValY}" Maximum="100" Background="{DynamicResource SubCardBg}" Foreground="{DynamicResource RedNeon}" BorderThickness="0"/>
                                </Grid>
                                <TextBlock Text="Right Stick" Foreground="{DynamicResource TextSecondary}" FontSize="10"/>
                                <Grid Margin="0,4,0,4">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding RightStickTextX}" Foreground="{DynamicResource TextSecondary}" FontSize="10" VerticalAlignment="Center"/>
                                    <ProgressBar Grid.Column="1" Height="4" Value="{Binding RightStickValX}" Maximum="100" Background="{DynamicResource SubCardBg}" Foreground="{DynamicResource RedNeon}" BorderThickness="0"/>
                                </Grid>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding RightStickTextY}" Foreground="{DynamicResource TextSecondary}" FontSize="10" VerticalAlignment="Center"/>
                                    <ProgressBar Grid.Column="1" Height="4" Value="{Binding RightStickValY}" Maximum="100" Background="{DynamicResource SubCardBg}" Foreground="{DynamicResource RedNeon}" BorderThickness="0"/>
                                </Grid>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
            
            <Grid Grid.Row="3" VerticalAlignment="Bottom" Margin="0,-30,0,0">
                <TextBlock Text="INPUT METHOD: DirectInput" Foreground="{DynamicResource TextSecondary}" FontSize="10" HorizontalAlignment="Left" Margin="0,0,0,5"/>
                <TextBlock Text="POLLING RATE: 1000Hz     LATENCY: 1ms     FIRMWARE: 2.15" Foreground="{DynamicResource TextSecondary}" FontSize="10" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock Text="ⓘ KEEP THIS WINDOW OPEN TO APPLY CHANGES" Foreground="{DynamicResource TextSecondary}" FontSize="10" HorizontalAlignment="Right" Margin="0,0,0,5"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>