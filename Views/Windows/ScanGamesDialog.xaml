<Window x:Class="DriftLift.Views.Windows.ScanGamesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Scan Installed Games" Height="580" Width="680"
        WindowStartupLocation="CenterOwner" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent">
    <Border Background="{DynamicResource BgDark}"
            BorderBrush="{DynamicResource RedNeon}"
            BorderThickness="1"
            CornerRadius="0">
        <Border.Effect>
            <DropShadowEffect Color="#FF1744" BlurRadius="25" ShadowDepth="0" Opacity="0.35"/>
        </Border.Effect>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,16" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="AUTO-DISCOVER INSTALLED GAMES" Foreground="{DynamicResource TextPrimary}" FontSize="15" FontWeight="Black"/>
                    <TextBlock x:Name="StatusText" Text="Searching Steam, EA, Epic Games, and system drives..." Foreground="{DynamicResource RedNeon}" FontSize="11" FontWeight="Bold" Margin="0,4,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Click="Cancel_Click" Style="{DynamicResource DefaultButtonStyle}" Padding="8,4" FontSize="11">
                    <TextBlock Text="✕" FontWeight="Bold"/>
                </Button>
            </Grid>

            <!-- Quick Filter Bar -->
            <Grid Grid.Row="1" Margin="0,0,0,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Click="SelectAll_Click" Content="SELECT ALL" Style="{DynamicResource DefaultButtonStyle}" Padding="10,4" FontSize="10" Margin="0,0,6,0"/>
                    <Button Click="DeselectAll_Click" Content="DESELECT ALL" Style="{DynamicResource DefaultButtonStyle}" Padding="10,4" FontSize="10"/>
                </StackPanel>
            </Grid>

            <!-- Content Area -->
            <Grid Grid.Row="2" Margin="0,0,0,16">
                <!-- Scanning Spinner Panel -->
                <StackPanel x:Name="ScanProgressPanel" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="🔍" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Scanning system libraries..." Foreground="{DynamicResource TextPrimary}" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <ProgressBar IsIndeterminate="True" Width="200" Height="4" Margin="0,10,0,0" Foreground="{DynamicResource RedNeon}"/>
                </StackPanel>

                <!-- No Games Detected -->
                <StackPanel x:Name="NoGamesText" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="No games automatically detected in standard directories." Foreground="{DynamicResource TextSecondary}" FontSize="12" HorizontalAlignment="Center"/>
                    <TextBlock Text="You can add any game .exe manually using the '+ ADD CUSTOM GAME' button." Foreground="{DynamicResource TextMuted}" FontSize="11" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                </StackPanel>

                <!-- Games List -->
                <ScrollViewer x:Name="GamesListScroll" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding DetectedGames}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="12,8" Margin="0,0,0,6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="38"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected, Mode=TwoWay}" Click="CheckBox_Click" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                        
                                        <!-- Game Icon -->
                                        <Grid Grid.Column="1" Width="30" Height="30" VerticalAlignment="Center">
                                            <Image Source="{Binding Icon}" Width="28" Height="28" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                        </Grid>

                                        <!-- Details -->
                                        <StackPanel Grid.Column="2" Margin="10,0,10,0" VerticalAlignment="Center">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Title}" Foreground="{DynamicResource TextPrimary}" FontSize="12" FontWeight="Bold"/>
                                                <Border Background="{DynamicResource SubCardBg}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0" Padding="6,1" Margin="8,0,0,0">
                                                    <TextBlock Text="{Binding Platform}" Foreground="{DynamicResource RedNeon}" FontSize="9" FontWeight="Bold"/>
                                                </Border>
                                            </StackPanel>
                                            <TextBlock Text="{Binding ExecutablePath}" Foreground="{DynamicResource TextMuted}" FontSize="10" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>

                                        <Border Grid.Column="3" Background="{DynamicResource SubCardBg}" CornerRadius="0" Padding="6,2" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Category}" Foreground="{DynamicResource TextSecondary}" FontSize="9" FontWeight="SemiBold"/>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>

            <!-- Bottom Actions -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Selected games will be added to your profile list with auto-switching enabled." Foreground="{DynamicResource TextMuted}" FontSize="10" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Click="Cancel_Click" Content="CANCEL" Style="{DynamicResource DefaultButtonStyle}" Padding="16,8" FontSize="11" Margin="0,0,8,0"/>
                <Button Grid.Column="2" x:Name="ImportBtn" Click="Import_Click" Content="IMPORT SELECTED GAMES" Style="{DynamicResource RedNeonBtn}" Padding="20,8" FontSize="11"/>
            </Grid>
        </Grid>
    </Border>
</Window>
